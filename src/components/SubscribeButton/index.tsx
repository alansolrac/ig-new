import styles from './styles.module.scss'

export function SusbriceButton() {
    return (
        <button
        type='button'
        className={styles.subscribeButton}
        >
            Subscribe now
        </button>
    )
}